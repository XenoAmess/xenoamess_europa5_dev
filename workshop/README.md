# Workshop 发布资料

每个产品使用 `workshop/<product_key>/` 保存：

- `description.bbcode`：商店说明唯一仓库真源。
- `media/`：经过来源、尺寸和人工画面审阅的发布素材。
- 可选的媒体清单、远端回读和发布政策。

Workshop 上传、页面编辑、可见性变化和媒体发布只在用户明确要求发布时执行。宣传图不能把 acceptance-only UI、测试文本或夹具状态冒充玩家正式体验。
